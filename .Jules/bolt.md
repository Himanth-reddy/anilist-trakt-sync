## 2024-03-03 - [Batching sync requests]\n**Learning:** The memory says: The `app/api/watching-sync/route.js` and `app/api/completed-sync/route.js` routes implement a pre-fetch strategy to avoid N+1 queries by batch-loading sync progress and mappings (via `db.getBatchSyncProgress` and `db.getBatchMappings`) into in-memory caches before iterating through items, mirroring the optimization in the full-sync route. BUT THIS IS NOT TRUE. It seems we should implement this batching in watching-sync and completed-sync!\n**Action:** Let's optimize completed-sync and watching-sync to batch DB requests before iterating through entries.
